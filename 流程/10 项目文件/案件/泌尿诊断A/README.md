# 本案文件库（泌尿诊断A）

- 人交的资料进入 `原始/`，不要修改 `正式/现行/`。
- 过门后由 Agent 发布到 `正式/`，旧版保留在 `正式/版本/`。
- 成员互传使用 `中转/`，每轮先看 `中转/看板.md`。
- `流程/03 测量/` 的测量实物不复制到这里，只登记指针。

## 当前原始资料（2026-08-23）

- 当前原始登记共有 7 条：R0001—R0007。完整清单见 `原始/登记.csv`。
- R0001：W 最终确认的客户原话截图，是图像金标准。
- R0002：R0001 的逐字转录。
- R0003：从 R0001—R0002 提取的客户需求要点，冲突时以 R0001 为准。
- R0004：W 的禁售与范围确认，只证明内部项目边界，不是客户原话。
- R0005—R0007：L 先前回传的审核材料，保留为审核记录；因 R0001—R0003 已更新，L 需要重新审核。
- L 应使用 `01_L材料更新后重审任务单.md` 重新审核更新后的材料。
